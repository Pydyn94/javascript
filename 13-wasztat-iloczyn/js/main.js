'use strict';

function dodawanieParametrow(param1, param2, param3) {
    var wynik = param1 + param2 + param3;
    return wynik;
}

var wynikMyFunction = dodawanieParametrow(10, 20, 30);
console.log(wynikMyFunction);
